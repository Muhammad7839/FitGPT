import React from "react";

export default function Dashboard() {
  return (
    <div className="dashboard">
      <h1 className="title">Dashboard</h1>
      <p className="subtitle">This is a placeholder. Later we’ll build the real recommendation UI.</p>
    </div>
  );
}
